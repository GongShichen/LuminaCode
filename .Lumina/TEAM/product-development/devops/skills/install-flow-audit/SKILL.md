---
name: Install Flow Audit
description: Verify build, install, resource copy, and uninstall behavior.
when-to-use: Use when `.Lumina` resources, Makefile, frontend, or backend install paths change.
user-invocable: false
context: inline
---

Audit:

- Built binaries and wrapper scripts.
- Resource copy into `~/.lumina`.
- Existing user config preservation.
- Backend endpoint cleanup.
- macOS/Linux and zsh/bash behavior.
